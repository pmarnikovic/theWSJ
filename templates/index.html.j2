<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>BWNewsHub</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="static/style.css">
</head>
<body>
    <header class="site-header">
        <div class="container">
            <!-- Fixed logo path -->
            <img src="static/assets/theWSJ%20Logo%20BWNewsHub.PNG" alt="BWNewsHub logo" class="site-logo">
            <h1>BWNewsHub</h1>
        </div>
    </header>

    <main class="content">
        <section class="articles">
            {% for article in articles %}
                <article class="article-card {{ article.style }}">
                    <img src="{{ article.image_url }}" alt="Article Image">
                    <h2>{{ article.title }}</h2>
                    <p>{{ article.summary }}</p>
                    <a href="{{ article.url }}" target="_blank">Read more</a>
                </article>
            {% endfor %}
        </section>
    </main>

    <footer class="site-footer">
        <div class="container">
            <p>&copy; 2025 BWNewsHub. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>
