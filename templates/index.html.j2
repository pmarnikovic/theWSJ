<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI REPORT</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #fff;
            color: #000;
            margin: 0;
            padding: 20px;
            text-align: center;
        }
        h1 {
            font-size: 48px;
            font-weight: bold;
            text-transform: uppercase;
            margin: 0 0 20px;
        }
        .main-headline {
            font-size: 36px;
            font-weight: bold;
            text-transform: uppercase;
            margin: 20px 0;
        }
        .main-headline a {
            color: #000;
            text-decoration: none;
        }
        .main-headline a:hover {
            text-decoration: underline;
        }
        .main-image {
            max-width: 600px;
            height: auto;
            margin: 10px auto;
            display: block;
        }
        .columns {
            display: flex;
            justify-content: space-around;
            text-align: left;
        }
        .column {
            width: 30%;
            padding: 10px;
        }
        .headline {
            font-size: 18px;
            font-weight: bold;
            text-transform: uppercase;
            margin: 5px 0;
        }
        .headline a {
            color: #0000ff;  /* Blue links like Drudge */
            text-decoration: none;
        }
        .headline a:hover {
            text-decoration: underline;
        }
        .headline img {
            max-width: 100%;
            height: auto;
            margin: 5px 0;
        }
    </style>
</head>
<body>
    <h1>AI REPORT</h1>
    
    {% if main_headline %}
    <div class="main-headline">
        <a href="{{ main_headline.url }}">{{ main_headline.headline }}</a>
    </div>
    {% if main_headline.image_url %}
    <img src="{{ main_headline.image_url }}" alt="Main article image" class="main-image">
    {% endif %}
    {% endif %}
    
    <div class="columns">
        <div class="column">
            {% for item in column1 %}
            <div class="headline">
                <a href="{{ item.url }}">{{ item.headline }}</a>
                {% if item.image_url %}
                <img src="{{ item.image_url }}" alt="Article image">
                {% endif %}
            </div>
            {% endfor %}
        </div>
        <div class="column">
            {% for item in column2 %}
            <div class="headline">
                <a href="{{ item.url }}">{{ item.headline }}</a>
                {% if item.image_url %}
                <img src="{{ item.image_url }}" alt="Article image">
                {% endif %}
            </div>
            {% endfor %}
        </div>
        <div class="column">
            {% for item in column3 %}
            <div class="headline">
                <a href="{{ item.url }}">{{ item.headline }}</a>
                {% if item.image_url %}
                <img src="{{ item.image_url }}" alt="Article image">
                {% endif %}
            </div>
            {% endfor %}
        </div>
    </div>
</body>
</html>
