<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>The Wall Street Junction</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="static/style.css">
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    
        <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-C1T5TPDXJX"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
    
      gtag('config', 'G-C1T5TPDXJX');
    </script>

    <!-- Dark theme overrides -->
    <style>
  :root { color-scheme: dark; }

  html, body {
    background: #000 !important;
    color: #e6e6e6;
  }

  h1, h2, h3 { color: #ffffff; }

  .site-header, .site-footer {
    background: #000;
    color: #e6e6e6;
  }

  .site-nav a {
    color: #e6e6e6;
    text-decoration: none;
  }
  .site-nav a:hover { color: #ffffff; }

  .article-card {
    background: #000;
    color: #e6e6e6;
    border: none;
    box-shadow: none;
  }

  .article-card img {
    display: block;
    max-width: 100%;
    height: auto;
  }

  a { color: #7ab6ff; }
  a:hover { color: #a8cfff; }

  .news-category { background: transparent; }

  .content {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 16px;
    align-items: start;
  }

  /* 🔁 Mobile override: switch to one column */
  @media (max-width: 768px) {
    .content {
      grid-template-columns: 1fr;
    }
  }

.site-header .container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 1em 0;
}

.site-logo {
  width: 100%;        /* fill the available width */
  max-width: 680px;   /* but never exceed 680px */
  height: auto;       /* keep aspect ratio */
  display: block;     /* allows centering with margin auto */
  margin: 0 auto 0.5em; /* center horizontally */
}


.site-nav ul {
  display: flex;
  justify-content: center;
  padding: 0;
  margin: 0.5em 0;
  list-style: none;
}

.site-nav li {
  margin: 0 1em;
}

ul li a {
  font-size: 2.0em;    /* increase font size */
}

#wall-street {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 20;
}

#wall-street h2 {
  text-decoration: underline;
}

#main-street {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 20;
}

#main-street h2 {
  text-decoration: underline;
}

#meme-street {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 20;
}

#meme-street h2 {
  text-decoration: underline;
}

#chatbot-widget {
  display: none;
}
#chatbot-widget[hidden] {
  display: none !important;
}

</style>


<script>
  // Prevent browser from restoring scroll position
  if ('scrollRestoration' in history) {
    history.scrollRestoration = 'manual';
  }

  // Force scroll to top on page load
  window.addEventListener('load', () => {
    window.scrollTo(0, 0);
    document.body.scrollTop = 0;            // Safari
    document.documentElement.scrollTop = 0; // Chrome, Firefox, Edge
  });
</script>



</head>
<body>  
    
     <!-- Scroll To Top Tab -->
<div id="scroll-tab" onclick="scrollToTop()">Scroll To Top</div>

<style>
  #scroll-tab {
    display: none; /* Hidden by default */
    position: fixed;
    top: 10px;
    left: 50%;
    transform: translateX(-50%);
    background-color: black;
    color: white;
    padding: 8px 16px;
    border-radius: 6px;
    border: 2px solid white;
    font-family: sans-serif;
    cursor: pointer;
    z-index: 1000;
    box-shadow: 0 2px 4px rgba(255,255,255,0.2);
    transition: background-color 0.3s ease;
  }

  #scroll-tab:hover {
    background-color: #000000;
  }
</style>

<script>
  function scrollToTop() {
    window.scrollTo({
      top: 0,
      behavior: 'smooth'
    });
  }

  // Show the tab after user scrolls ~600px
  window.addEventListener('scroll', () => {
    const scrollTab = document.getElementById('scroll-tab');
    if (window.scrollY > 600) {
      scrollTab.style.display = 'block';
    } else {
      scrollTab.style.display = 'none';
    }
  });
</script>


    
    <header class="site-header">
        <div class="container">
            <img src="images/Logo02.png" alt="The Wall Street Junction" class="site-logo">
<!-- Hide Temporarily
            <nav class="site-nav">
                <ul>
                    <li><a href="#wall-street">Wall Street</a></li>
                    <li><a href="#main-street">Main Street</a></li>
                    <li><a href="#meme-street">Meme Street</a></li>
                </ul>
            </nav>
 -->
        </div>
    </header>

<div style="height: 20px;"></div>

    <main class="content">
        <section id="wall-street" class="news-category">
            <h2>Wall Street</h2>
            <div class="articles">
                {% for article in articles if article.category == 'wall' %}
                <a href="{{ article.url }}" target="_blank"><article class="article-card {{ article.style }}">
                    <img src="{{ article.image_url }}" alt="Article Image">
                    <h3>{{ article.title }}</h3>
                </article></a>
                {% endfor %}
            </div>
        </section>

<!-- Hide Temporarily <section id="main-street" class="news-category">
            <h2>Main Street</h2>
            <div class="articles">
                {% for article in articles if article.category == 'main' %}
                <a href="{{ article.url }}" target="_blank"><article class="article-card {{ article.style }}">
                    <img src="{{ article.image_url }}" alt="Article Image">
                    <h3>{{ article.title }}</h3>
                </article></a>
                {% endfor %}
            </div>
        </section>

        <section id="meme-street" class="news-category">
            <h2>Meme Street</h2>
            <div class="articles">
                {% for article in articles if article.category == 'meme' %}
                <a href="{{ article.url }}" target="_blank"><article class="article-card {{ article.style }}">
                    <img src="{{ article.image_url }}" alt="Article Image">
                    <h3>{{ article.title }}</h3>
                </article></a>
                {% endfor %}
            </div>
        </section>
  -->  
    </main>


<!-- Chatbot trigger button -->
<button id="chatbot-toggle"
        aria-haspopup="dialog"
        aria-controls="chatbot-widget"
        aria-expanded="false"
        title="Open chat">
  💬
</button>

<!-- Chatbot widget -->
<div id="chatbot-widget" role="dialog" aria-label="Chat with us" aria-modal="false" hidden>
  <header class="cb-header">
    <strong>ASK ME ANYTHING!</strong>
    <button class="cb-close" aria-label="Close chat">×</button>
  </header>

  <div id="chatbot-messages" class="cb-messages" aria-live="polite"></div>

  <form id="chatbot-form" class="cb-form" autocomplete="off">
    <input id="chatbot-input"
           type="text"
           name="message"
           placeholder="Type a message..."
           aria-label="Type your message"
           required />
    <button type="submit" class="cb-send">Send</button>
  </form>
</div>

<style>
  :root {
    --cb-bg: #111;          /* widget background */
    --cb-fg: #fff;          /* text color */
    --cb-accent: #ffffff;   /* borders/icons */
    --cb-shadow: rgba(0,0,0,0.35);
  }

  /* Floating toggle button */
  #chatbot-toggle {
    position: fixed;
    right: max(16px, env(safe-area-inset-right));
    bottom: calc(max(16px, env(safe-area-inset-bottom)) + 190px);
    width: 52px;
    height: 52px;
    border-radius: 50%;
    background: #000;
    color: #fff;
    border: 2px solid #fff;
    box-shadow: 0 6px 16px var(--cb-shadow);
    cursor: pointer;
    z-index: 1000;
    font-size: 22px;
    line-height: 1;
    display: grid;
    place-items: center;
    transition: transform .15s ease, background-color .2s ease;
  }
  #chatbot-toggle:hover { background: #222; transform: translateY(-1px); }
  #chatbot-toggle:active { transform: translateY(0); }

  /* Widget container */
  #chatbot-widget {
    position: fixed;
    right: max(16px, env(safe-area-inset-right));
    bottom: calc(max(16px, env(safe-area-inset-bottom)) + 0px);
    width: min(360px, 92vw);
    max-height: min(70vh, 560px);
    display: flex;
    flex-direction: column;
    background: var(--cb-bg);
    color: var(--cb-fg);
    border: 2px solid var(--cb-accent);
    border-radius: 12px;
    box-shadow: 0 16px 40px var(--cb-shadow);
    overflow: hidden;
    z-index: 1000;
  }

  .cb-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 12px;
    border-bottom: 1px solid var(--cb-accent);
    background: #000;
  }
  .cb-close {
    background: transparent;
    color: var(--cb-fg);
    border: 1px solid var(--cb-accent);
    border-radius: 6px;
    width: 28px; height: 28px;
    line-height: 24px;
    cursor: pointer;
  }

  .cb-messages {
    padding: 12px;
    overflow-y: auto;
    gap: 8px;
    display: flex;
    flex-direction: column;
  }

  .cb-msg {
    max-width: 85%;
    padding: 8px 10px;
    border-radius: 10px;
    line-height: 1.3;
    word-wrap: break-word;
    white-space: pre-wrap;
    border: 1px solid rgba(255,255,255,0.2);
  }
  .cb-msg.user   { align-self: flex-end; background: #1f2937; }
  .cb-msg.bot    { align-self: flex-start; background: #0b0b0b; }
  .cb-msg.system { align-self: center; font-size: 18px; opacity: 0.2; background: transparent; border: none; }

  .cb-form {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 8px;
    padding: 10px 12px;
    border-top: 1px solid var(--cb-accent);
    background: #000;
  }
  #chatbot-input {
    background: #0d0d0d;
    color: var(--cb-fg);
    border: 1px solid #333;
    border-radius: 8px;
    font-size: 16px;
    padding: 10px 12px;
    outline: none;
  }
  #chatbot-input:focus { border-color: #666; }
  .cb-send {
    background: #000;
    color: var(--cb-fg);
    border: 2px solid var(--cb-accent);
    border-radius: 8px;
    padding: 0 14px;
    cursor: pointer;
  }
  .cb-send:hover { background: #111; }
</style>

<script>
  const elToggle   = document.getElementById('chatbot-toggle');
  const elWidget   = document.getElementById('chatbot-widget');
  const elClose    = elWidget.querySelector('.cb-close');
  const elForm     = document.getElementById('chatbot-form');
  const elInput    = document.getElementById('chatbot-input');
  const elMessages = document.getElementById('chatbot-messages');

  function setOpen(isOpen) {
    if (isOpen) {
      elWidget.hidden = false;
      elToggle.setAttribute('aria-expanded', 'true');
      // Focus input after paint
      setTimeout(() => elInput.focus(), 0);
    } else {
      elWidget.hidden = true;
      elToggle.setAttribute('aria-expanded', 'false');
      elToggle.focus();
    }
  }


  function appendMessage(text, role = 'bot') {
    const div = document.createElement('div');
    div.className = `cb-msg ${role}`;
    div.textContent = text;
    elMessages.appendChild(div);
    elMessages.scrollTop = elMessages.scrollHeight;
  }

  // Demo system greeting
  appendMessage('ChatBot Powered by "null"', 'system');

  elToggle.addEventListener('click', () => setOpen(elWidget.hidden));
  elClose.addEventListener('click', () => setOpen(false));
  document.addEventListener('keydown', (e) => {
    if (!elWidget.hidden && e.key === 'Escape') setOpen(false);
  });

  elForm.addEventListener('submit', async (e) => {
    e.preventDefault();
    const msg = elInput.value.trim();
    if (!msg) return;
    appendMessage(msg, 'user');
    elInput.value = '';

    // Show typing indicator
    const typing = document.createElement('div');
    typing.className = 'cb-msg system';
    typing.textContent = '…';
    elMessages.appendChild(typing);
    elMessages.scrollTop = elMessages.scrollHeight;

    try {
      // TODO: replace with your API endpoint
      // Example expects POST /api/chat { message } -> { reply }
      const res = await fetch('/api/chat', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({ message: msg })
      });

      let reply;
      if (res.ok) {
        const data = await res.json();
        reply = data.reply || 'OK.';
      } else {
        reply = 'Sorry, there was an issue responding.';
      }

      typing.remove();
      appendMessage(reply, 'bot');
    } catch (err) {
      typing.remove();
      appendMessage('Network error. Please try again.', 'bot');
    }
  });
</script>



    <footer class="site-footer">
        <div class="container">
            <p>&copy; 2025 The Wall Street Junction. All rights reserved. <a href="@theWSJ.ai">Contact Us</a>
                <h2>Top 25 AI Websites</h2>
                <ul>
                    <li><a href="https://openai.com" target="_blank" rel="noopener noreferrer">OpenAI</a></li>
                    <li><a href="https://ai.google" target="_blank" rel="noopener noreferrer">Google AI</a></li>
                    <li><a href="https://www.microsoft.com/en-us/ai" target="_blank" rel="noopener noreferrer">Microsoft AI</a></li>
                    <li><a href="https://huggingface.co" target="_blank" rel="noopener noreferrer">Hugging Face</a></li>
                    <li><a href="https://techcrunch.com/category/artificial-intelligence/" target="_blank" rel="noopener noreferrer">TechCrunch AI</a></li>
                    <li><a href="https://venturebeat.com/category/ai/" target="_blank" rel="noopener noreferrer">VentureBeat AI</a></li>
                    <li><a href="https://www.wired.com/tag/artificial-intelligence/" target="_blank" rel="noopener noreferrer">Wired AI</a></li>
                    <li><a href="https://www.technologyreview.com/topic/artificial-intelligence/" target="_blank" rel="noopener noreferrer">MIT Technology Review</a></li>
                    <li><a href="https://aimagazine.com" target="_blank" rel="noopener noreferrer">AI Magazine</a></li>
                    <li><a href="https://www.analyticsvidhya.com" target="_blank" rel="noopener noreferrer">Analytics Vidhya</a></li>
                    <li><a href="https://www.aitrends.com" target="_blank" rel="noopener noreferrer">AI Trends</a></li>
                    <li><a href="https://news.mit.edu/topic/artificial-intelligence2" target="_blank" rel="noopener noreferrer">MIT News AI</a></li>
                    <li><a href="https://www.dataversity.net" target="_blank" rel="noopener noreferrer">Dataversity</a></li>
                    <li><a href="https://emerj.com" target="_blank" rel="noopener noreferrer">Emerj</a></li>
                    <li><a href="https://www.kdnuggets.com" target="_blank" rel="noopener noreferrer">KDnuggets</a></li>
                    <li><a href="https://www.analyticsinsight.net" target="_blank" rel="noopener noreferrer">Analytics Insight</a></li>
                    <li><a href="https://insidebigdata.com" target="_blank" rel="noopener noreferrer">Inside Big Data</a></li>
                    <li><a href="https://www.datasciencecentral.com" target="_blank" rel="noopener noreferrer">Data Science Central</a></li>
                    <li><a href="https://developer.ibm.com" target="_blank" rel="noopener noreferrer">IBM Developer</a></li>
                    <li><a href="https://intelligence.org" target="_blank" rel="noopener noreferrer">Machine Intelligence Research Institute</a></li>
                    <li><a href="https://paperswithcode.com" target="_blank" rel="noopener noreferrer">Papers with Code</a></li>
                    <li><a href="https://www.anthropic.com" target="_blank" rel="noopener noreferrer">Anthropic</a></li>
                    <li><a href="https://stability.ai" target="_blank" rel="noopener noreferrer">Stability AI</a></li>
                    <li><a href="https://www.perplexity.ai" target="_blank" rel="noopener noreferrer">Perplexity AI</a></li>
                    <li><a href="https://www.artificialintelligence-news.com" target="_blank" rel="noopener noreferrer">Artificial Intelligence News</a></li>
                </ul>
            </p>
        </div>
    </footer>
</body>
</html>
