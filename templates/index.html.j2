<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>The Wall Street Junction</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="static/style.css">
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-5QVFJX51QK"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-5QVFJX51QK');
    </script>

    <!-- Dark theme overrides -->
    <style>
      :root { color-scheme: dark; }

      html, body {
        background: #000 !important;
        color: #e6e6e6;
      }

      h1, h2, h3 { color: #ffffff; }

      /* Ensure header/footer blend with dark background */
      .site-header, .site-footer {
        background: #000;
        color: #e6e6e6;
      }

      /* Navigation links */
      .site-nav a {
        color: #e6e6e6;
        text-decoration: none;
      }
      .site-nav a:hover { color: #ffffff; }

      /* Article cards on dark */
      .article-card {
        background: #111;
        color: #e6e6e6;
        border: 1px solid #222;
        box-shadow: none;
      }

      /* Images should not invert; keep default */
      .article-card img {
        display: block;
        max-width: 100%;
        height: auto;
      }

      /* Links inside content */
      a { color: #7ab6ff; }
      a:hover { color: #a8cfff; }

      /* Optional: sections transparent over black */
      .news-category { background: transparent; }

      /* Place the three sections side-by-side */
      .content {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 16px;
        align-items: start;
      }

        #wall-street.news-category {
      width: 33.33%;
      margin: 0 auto;
    }
    
    #main-street.news-category {
      width: 33.33%;
      margin: 0 auto;
    }
    
    #meme-street.news-category {
      width: 33.33%;
      margin: 0 auto;
    }
    
    </style>
</head>
<body>
    <header class="site-header">
        <div class="container">
            <img src="images/Logo02.png" alt="The Wall Street Junction" class="site-logo">
            <h1>The Wall Street Junction</h1>
            <nav class="site-nav">
                <ul>
                    <li><a href="#wall-street">Wall Street</a></li>
                    <li><a href="#main-street">Main Street</a></li>
                    <li><a href="#meme-street">Meme Street</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main class="content">
        <section id="wall-street" class="news-category">
            <h2>Wall Street</h2>
            <div class="articles">
                {% for article in articles if article.category == 'wall' %}
                <article class="article-card {{ article.style }}">
                    <img src="{{ article.image_url }}" alt="Article Image">
                    <h3>{{ article.title }}</h3>
                    <p>{{ article.summary }}</p>
                    <a href="{{ article.url }}" target="_blank">Read more</a>
                </article>
                {% endfor %}
            </div>
        </section>

        <section id="main-street" class="news-category">
            <h2>Main Street</h2>
            <div class="articles">
                {% for article in articles if article.category == 'main' %}
                <article class="article-card {{ article.style }}">
                    <img src="{{ article.image_url }}" alt="Article Image">
                    <h3>{{ article.title }}</h3>
                    <p>{{ article.summary }}</p>
                    <a href="{{ article.url }}" target="_blank">Read more</a>
                </article>
                {% endfor %}
            </div>
        </section>

        <section id="meme-street" class="news-category">
            <h2>Meme Street</h2>
            <div class="articles">
                {% for article in articles if article.category == 'meme' %}
                <article class="article-card {{ article.style }}">
                    <img src="{{ article.image_url }}" alt="Article Image">
                    <h3>{{ article.title }}</h3>
                    <p>{{ article.summary }}</p>
                    <a href="{{ article.url }}" target="_blank">Read more</a>
                </article>
                {% endfor %}
            </div>
        </section>
    </main>

    <footer class="site-footer">
        <div class="container">
            <p>&copy; 2025 BWNewsHub. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>
